sudo: required
services:
  - docker

before_install:
  - docker build -t docker_master/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true docker_master/docker-react npm run test -- --coverage
